
### 工程文件中配置中增加相关的目录。

工程文件的路径:
```
.idea => xxx.iml文件
```

增加路径：
```
<excludeFolder url="file://$MODULE_DIR$/build" /> // 排除build目录
<excludeFolder url="file://$MODULE_DIR$/examples" /> // 排除examples目录
```
